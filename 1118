// 1. If a is even, add b to a
        if (a % 2 == 0) {
            a += b;
        }

        // 2. If b is odd, multiply c by 2
        if (b % 2 != 0) {
            c *= 2;
        }

        // 3. If c is a multiple of 3, add a to c
        if (c % 3 == 0) {
            c += a;
        }

        // 4. If the sum of a, b, and c is greater than 100, subtract 100 from each
        if (a + b + c > 100) {
            a -= 100;
            b -= 100;
            c -= 100;
        }
