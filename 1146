people.sort((p1, p2) -> {
            if (p1.age != p2.age) {
                return Integer.compare(p2.age, p1.age);
            }
            return p1.name.compareTo(p2.name);
        });
        
